#include "main.h"
/**
 * puts_half - prints half of a string followed by new linw
 * @str: pointer string
 * Return: always 0 success
 */
void puts_half(char *str)
{
	int length, h, i;

	for (length = 0; str[length] != '\0'; length++)
	{
	}

	h = (lenght - 1) / 2
	
	for (i = h + 1; str[i] != '\0'; i++)
	{
	
	_putchar(str[i]);
	}
	_putchar('\n');

}
