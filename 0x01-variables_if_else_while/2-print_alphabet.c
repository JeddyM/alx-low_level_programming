#include <stdio.h>
/**
 * main - prints the alphabet letters in lowercase
 * and prints a new line
 * Return : always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 25; ++i)
	putchar('a' + i);

	putchar("\n");
	return (0)
}	
