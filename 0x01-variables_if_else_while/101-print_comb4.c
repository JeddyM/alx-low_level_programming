#include <stdio.h>
/**
 * main -Entry point prints 3 digit numbers
 * ascending order
 * Return : always 0 (success)
 */

int main(void)
{

	int a,
	int b,
	int c,

	for (a = 48; n < 58; a++)
	{
		for(b = 49; b < 58; b++)
		{

			for (c = 50; c < 58; c++)
			{

				if (a < b && b < c)
				{
					putchar(a);
					putchar(b);
					putchar(c);
					if (a != 50 || m != 56)
					{
					putchar(',');
					putchar(' ');
					}
				}
			}
		}

	}

	putchar('\n');
	return (0);
}



























}
